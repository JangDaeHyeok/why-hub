# 런타임 의존성 (확정 스택 · CLAUDE.md §3). Phase별로 실제 사용 시점 표기.
fastmcp>=3.4,<4  # MCP 인터페이스 (P07) — 검증한 auth API 범위로 핀
fastapi    # HTTP 인터페이스 (P08)
uvicorn    # HTTP 서버 구동 (P08)
jinja2         # 읽기 UI 템플릿 (M6)
markdown-it-py # 마크다운 서버 렌더 (M6)
python-multipart  # 쓰기/생성 UI 폼 파싱 (M7·M8)
pyyaml     # frontmatter/이력 YAML (P02~)
httpx      # HTTP 테스트 (P08) + LLM 엔드포인트 클라이언트 (P12)
psycopg[binary]  # PostgreSQL 저장 백엔드 (배포 — storage.backend=postgres)
argon2-cffi      # 비밀번호 Argon2id 해시 (인증)
pyjwt[crypto]    # RS256 JWT 발급·검증 (인증)

# 개발
pytest>=8
